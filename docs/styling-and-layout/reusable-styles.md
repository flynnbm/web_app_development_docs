# Create reusable styles and color variables

Placeholder for defining styles once and reusing them across multiple page
elements.

The completed guide will introduce a small CSS organization and show how a
named value such as a background color can be changed in one place and reused
by several styles.
